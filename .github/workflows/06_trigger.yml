name: Trigger
run-name: Running - Trigger
on: [push, workflow_dispatch]

jobs:
    on-push:
        runs-on: ubuntu-latest
        steps:
            - name: comentário
              run: echo "Trigger realizada"